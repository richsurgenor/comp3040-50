FUNC void displayvalues(int oldkey, int newkey) {
	double old =  ( (TIM10->ARR+1) * (oldkey * 10) ) / 100;
  double new = 	( (TIM10->ARR+1) * (newkey * 10) ) / 100;

  difference = new - old;

  if (difference < 0) {
    difference = difference * -1;
  }

  double delta = ( 1 / ((double) TIMX_CLOCK_SPEED / difference) / (1+TIM10->PSC) ) ;

  exec(log > "myvalues.csv");
  for (int i = 0; i < 200; i++) {
    printf("%lf, ", data[i]);
  }
}
